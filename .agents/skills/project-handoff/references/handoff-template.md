# docs/agent/handoffs/{task-slug}.md 模板

## 任务标识

- task-slug：`<kebab-case-task-slug>`
- 最后更新时间：`YYYY-MM-DD HH:mm`
- 当前线程/责任人：`<可选>`

## 当前开发目标

- `<本轮要达成的目标>`

## 当前分支

- `<git branch>`

## 当前状态

- `<进行中 / 阻塞 / 待验证 / 待发布>`

## 已完成

- [ ] `<已完成项 1>`
- [ ] `<已完成项 2>`

## 正在处理

- `<当前正在处理的问题或任务>`

## 未完成

- [ ] `<未完成项 1>`
- [ ] `<未完成项 2>`

## 最近修改文件

- `<路径 1>`：`<修改目的>`
- `<路径 2>`：`<修改目的>`

## 关键业务规则

- `<必须遵守的业务规则 1>`
- `<必须遵守的业务规则 2>`

## 已踩过的坑

- `<坑点描述>`
- `<规避方式>`

## 下一个线程启动步骤

1. 读取 `AGENTS.md`、`docs/agent/PROJECT_CONTEXT.md`、`docs/agent/BUSINESS_CONTEXT.md`。
2. 读取本文件与相关 ADR 文件（`docs/agent/decisions/`）。
3. 执行 `git status`，确认工作区。
4. 按“未完成”从上到下继续。
5. 完成后更新本文件与 `docs/agent/task-history/YYYY-MM-DD-{task-slug}.md`。

## 当前构建/测试状态

- Backend：`<通过 / 失败 / 未执行>`
- Frontend：`<通过 / 失败 / 未执行>`
- 备注：`<失败原因或未执行原因>`

## 需要用户确认的问题

- [ ] `<待用户确认的问题 1>`
- [ ] `<待用户确认的问题 2>`
