# BUSINESS_CONTEXT.md 模板（工业调度项目）

> 仅记录稳定业务背景与长期规则，不写入临时开发状态、短期任务进度、线程备注。

## 系统边界

- 本系统负责：`<任务编排 / 调度决策 / 执行跟踪 / 异常告警>`
- 本系统不负责：`<明确外部系统职责边界>`
- 上游系统：`<MES/WMS/ERP/...>`
- 下游系统：`RCS / AGV / AMA / TM / STK / PLC`

## 核心业务对象

- 调度任务（Task）：`<字段、唯一键、约束>`
- 运输单元（Carrier/Pod/Pallet）：`<标识与状态>`
- 设备资源（AGV/STK/PLC 点位）：`<能力与限制>`
- 工位/库位（Station/Bin）：`<占用规则>`
- 指令单（Command/Job）：`<生命周期>`

## 任务状态流转

- 建议状态：`Created -> Queued -> Dispatched -> Executing -> Completed`
- 异常状态：`Failed / Canceled / Timeout / Compensating`
- 流转约束：
  - `<哪些状态可逆，哪些不可逆>`
  - `<重试次数与重试窗口>`
  - `<人工介入触发条件>`

## AMA / TM / STK / PLC / RCS 的职责

- AMA：`<上层业务编排/工艺协调职责>`
- TM：`<任务管理、优先级、队列策略>`
- STK：`<立库入出库执行与反馈>`
- PLC：`<设备控制信号、互锁、安全条件>`
- RCS：`<AGV调度、路径、车辆状态回传>`

## 第三方接口约定

- 通信协议：`HTTP / gRPC / MQTT / TCP / OPC UA / ...`
- 接口幂等键：`<字段>`
- 超时与重试：`<策略>`
- 回调/轮询：`<机制>`
- 错误码语义：`<可追溯文档位置>`

## 异常处理规则

- 指令下发失败：`<回滚/补偿策略>`
- 第三方超时：`<重试 + 告警 + 人工介入门槛>`
- 状态不一致：`<对账策略与修复流程>`
- 设备故障：`<隔离、降级、恢复条件>`

## 不能随意改动的业务规则

- `<规则 1：例如任务状态机不可跳转>`
- `<规则 2：例如关键库存位必须强一致>`
- `<规则 3：例如设备安全互锁逻辑不可绕过>`
- `<规则 4：例如外部协议字段兼容策略>`

## 维护约束

- 若需修改本文件，必须先说明“为什么属于稳定业务规则变更”，并等待用户确认。
- 当前任务进度请写入 `docs/agent/handoffs/{task-slug}.md`。
